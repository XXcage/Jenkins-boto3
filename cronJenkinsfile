pipeline {
    agent any
    triggers {
        cron('*/5 * * * *')
    }
    stages {
        stage('Run Latest Docker Image') {
            environment {
                DOCKER_IMAGE = 'mydockerhubusername/myimage:latest'
            }
            steps {
                script {
                    def imageName = DOCKER_IMAGE.split(':')[0]
                    def latestTag = sh(script: "docker search --format '{{.Tag}}' ${imageName} | sort -r | head -n 1", returnStdout: true).trim()
                    def latestImage = "${imageName}:${latestTag}"
                    
                    docker.image(latestImage).pull()
                    docker.image(latestImage).run()
                }
            }
        }
    }
}
